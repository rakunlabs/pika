import{_ as s,o as i,c as a,ak as t}from"./chunks/framework.WEfAUR5x.js";const k=JSON.parse('{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1780002933000}'),n={name:"guide/configuration.md"};function d(o,e,r,l,h,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>Pika has two layers of configuration:</p><ol><li><strong>Process-level config</strong> — supplied at startup via environment variables, a YAML/TOML/JSON file, or one of the supported external sources (Vault, Consul, GCP Parameter/Secret Manager, HTTP). This covers things that must be known before the server is up: the listen port, TLS file paths, the storage path, cluster membership, and the optional at-rest encryption passphrase.</li><li><strong>Runtime settings</strong> — stored in the database and edited from the <strong>Settings</strong> pages of the UI. This covers everything else: certificate rotation, HTTP fallback policy, authentication strategies, external resources, hooks, public-port compatibility endpoints, and so on. Changes apply without restarting unless noted.</li></ol><p>This page documents the first layer.</p><h2 id="how-config-is-loaded" tabindex="-1">How config is loaded <a class="header-anchor" href="#how-config-is-loaded" aria-label="Permalink to “How config is loaded”">​</a></h2><p>Process-level config is loaded with <a href="https://github.com/rakunlabs/chu" target="_blank" rel="noreferrer"><code>rakunlabs/chu</code></a>. The configuration name passed to chu is <code>pika</code>. On every start, chu walks a fixed list of loaders in order and merges what each one returns into the same <code>Config</code> struct — later loaders override fields set by earlier ones:</p><table tabindex="0"><thead><tr><th>#</th><th>Loader</th><th>Trigger</th><th>What it provides</th></tr></thead><tbody><tr><td>1</td><td><code>default</code></td><td>Always runs</td><td>Struct-tag <code>default:&quot;…&quot;</code> values (e.g. <code>server.port=8080</code>, <code>cluster.port=5000</code>).</td></tr><tr><td>2</td><td><code>consul</code></td><td><code>CONSUL_HTTP_ADDR</code> set</td><td>YAML payload from Consul KV at key <code>pika</code> (optionally prefixed).</td></tr><tr><td>3</td><td><code>vault</code></td><td><code>VAULT_ADDR</code> or <code>VAULT_AGENT_ADDR</code> set, plus <code>VAULT_SECRET_BASE_PATH</code></td><td>KVv2 secret at <code>&lt;base&gt;/pika</code>.</td></tr><tr><td>4</td><td><code>gcpparameter</code></td><td><code>GCP_PROJECT_ID</code> set</td><td>GCP Parameter Manager parameter named <code>pika</code>.</td></tr><tr><td>5</td><td><code>gcpsecret</code></td><td><code>GCP_PROJECT_ID</code> set</td><td>GCP Secret Manager secret named <code>pika</code> (+ optional shared/generic secrets).</td></tr><tr><td>6</td><td><code>http</code></td><td><code>CONFIG_HTTP_ADDR</code> set</td><td><code>GET {CONFIG_HTTP_ADDR}/pika</code> returning JSON/YAML/TOML.</td></tr><tr><td>7</td><td><code>file</code></td><td><code>CONFIG_FILE=/path/to/pika.yaml</code> set, or <code>pika.{toml,yaml,yml,json}</code> in the working directory or <code>/etc/</code></td><td>The on-disk config file.</td></tr><tr><td>8</td><td><code>env</code></td><td>Always runs</td><td>Process environment variables with the <code>PIKA_</code> prefix.</td></tr></tbody></table><p>Notes:</p><ul><li>The default order intentionally puts <code>env</code> last so that an operator can always override anything else (e.g. force <code>PIKA_LOG_LEVEL=debug</code> for a single boot).</li><li>Each external loader auto-skips if its trigger env var is missing, so you only pay for what you use. There is <strong>no</strong> hard dependency on Consul/Vault/GCP libraries at runtime — the clients are only constructed when their env vars are set.</li><li>The <code>default</code> tag does not &quot;fill in&quot; zero values after another loader has set the struct field. It only seeds fields nothing else touched.</li><li>Sensitive fields tagged <code>log:&quot;-&quot;</code> (<code>encryption.password</code>, <code>cluster.security_key</code>) are masked from the <code>loaded configuration</code> log line printed at startup.</li></ul><h3 id="when-to-use-which-source" tabindex="-1">When to use which source <a class="header-anchor" href="#when-to-use-which-source" aria-label="Permalink to “When to use which source”">​</a></h3><ul><li><strong>YAML/TOML file</strong> — local dev, baked container images, Kubernetes ConfigMap mounts.</li><li><strong><code>PIKA_*</code> env vars</strong> — Docker Compose, Kubernetes Deployments, ad-hoc one-shot overrides.</li><li><strong>Vault / Consul</strong> — central, audited bootstrap config in environments where you already operate one of those.</li><li><strong>GCP Parameter / Secret Manager</strong> — same idea on GCP. Parameter Manager renders templated payloads server-side; Secret Manager stores opaque blobs and supports a list of shared/generic secrets that get merged in BEFORE the app-specific one.</li><li><strong>HTTP</strong> — point pika at any internal config server speaking JSON/YAML/TOML.</li></ul><h2 id="environment-variables" tabindex="-1">Environment variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to “Environment variables”">​</a></h2><p>The most common variables. All <code>PIKA_*</code> env vars use the <code>PIKA_</code> prefix and <code>_</code> for nesting; the underlying tag is <code>cfg</code>, so <code>server.tls.cert_file</code> becomes <code>PIKA_SERVER_TLS_CERT_FILE</code>.</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>PIKA_LOG_LEVEL</code></td><td><code>info</code></td><td><code>debug</code>, <code>info</code>, <code>warn</code>, or <code>error</code>.</td></tr><tr><td><code>PIKA_SERVER_HOST</code></td><td><em>(all interfaces)</em></td><td>Bind address for the admin server.</td></tr><tr><td><code>PIKA_SERVER_PORT</code></td><td><code>8080</code></td><td>Listen port (HTTPS admin UI + authenticated <code>/data/*</code>).</td></tr><tr><td><code>PIKA_SERVER_BASE_PATH</code></td><td><code>/</code></td><td>Base URL path — set when running behind a sub-path.</td></tr><tr><td><code>PIKA_SERVER_TLS_ENABLED</code></td><td><code>true</code></td><td>Enable HTTPS support on the main listener.</td></tr><tr><td><code>PIKA_SERVER_TLS_CERT_FILE</code></td><td><em>(auto-managed)</em></td><td>PEM certificate path. Empty = managed cert under the storage directory.</td></tr><tr><td><code>PIKA_SERVER_TLS_KEY_FILE</code></td><td><em>(auto-managed)</em></td><td>PEM private key path. Empty = managed key under the storage directory.</td></tr><tr><td><code>PIKA_STORAGE_BW_PATH</code></td><td><code>data/pika</code></td><td>Embedded BadgerDB directory.</td></tr><tr><td><code>PIKA_STORAGE_BW_IN_MEMORY</code></td><td><code>false</code></td><td>Run the BadgerDB backend entirely in memory (tests/CI).</td></tr><tr><td><code>PIKA_ENCRYPTION_PASSWORD</code></td><td></td><td>Optional at-rest passphrase for auto-unlock/auto-initialize. See <a href="./encryption">Encryption</a>.</td></tr><tr><td><code>PIKA_CLUSTER_ENABLED</code></td><td><code>false</code></td><td>Enable clustering.</td></tr><tr><td><code>PIKA_CLUSTER_DNS_ADDR</code></td><td></td><td>DNS name resolving to all peer IPs.</td></tr><tr><td><code>PIKA_CLUSTER_REPLICAS</code></td><td></td><td>Number of cluster members (must match reality).</td></tr><tr><td><code>PIKA_CLUSTER_PORT</code></td><td><code>5000</code></td><td>UDP/QUIC peer port.</td></tr><tr><td><code>PIKA_CLUSTER_SECURITY_KEY</code></td><td></td><td>Pre-shared key — must match across peers.</td></tr></tbody></table><p>Loader-control env vars (not prefixed with <code>PIKA_</code>, consumed directly by chu):</p><table tabindex="0"><thead><tr><th>Variable</th><th>Loader</th><th>Description</th></tr></thead><tbody><tr><td><code>CONFIG_FILE</code></td><td><code>file</code></td><td>Absolute path to the config file. Overrides folder/extension search.</td></tr><tr><td><code>CONFIG_ENV_FILE</code></td><td><code>env</code></td><td>Extra <code>.env</code>-style file to load before reading the environment.</td></tr><tr><td><code>CONFIG_HTTP_ADDR</code></td><td><code>http</code></td><td>Base URL of an HTTP config server. The loader fetches <code>GET {addr}/pika</code>.</td></tr><tr><td><code>CONFIG_HTTP_SUFFIX</code></td><td><code>http</code></td><td>Optional path suffix appended after <code>/pika</code>.</td></tr><tr><td><code>CONFIG_HTTP_QUERY</code></td><td><code>http</code></td><td>Optional query string appended to the request.</td></tr><tr><td><code>CONSUL_HTTP_ADDR</code></td><td><code>consul</code></td><td>Consul HTTP API address. Missing = loader skipped.</td></tr><tr><td><code>CONSUL_CONFIG_PATH_PREFIX</code></td><td><code>consul</code></td><td>KV prefix prepended to the config name. Final key: <code>&lt;prefix&gt;/pika</code>.</td></tr><tr><td><code>VAULT_ADDR</code> / <code>VAULT_AGENT_ADDR</code></td><td><code>vault</code></td><td>Vault server address. Missing both = loader skipped.</td></tr><tr><td><code>VAULT_SECRET_BASE_PATH</code></td><td><code>vault</code></td><td>KVv2 mount path. Required when the loader runs.</td></tr><tr><td><code>VAULT_ROLE_ID</code>, <code>VAULT_ROLE_SECRET</code></td><td><code>vault</code></td><td>AppRole credentials. Optional — Vault Agent / token auth also work.</td></tr><tr><td><code>VAULT_APPROLE_BASE_PATH</code></td><td><code>vault</code></td><td>AppRole login path. Default <code>auth/approle/login</code>.</td></tr><tr><td><code>GCP_PROJECT_ID</code></td><td><code>gcpparameter</code>, <code>gcpsecret</code></td><td>GCP project id. Missing = both GCP loaders skip.</td></tr><tr><td><code>GCP_PARAMETER_LOCATION</code></td><td><code>gcpparameter</code></td><td>Default <code>global</code>. Use a region for regional parameters.</td></tr><tr><td><code>GCP_PARAMETER_VERSION</code></td><td><code>gcpparameter</code></td><td>Default <code>latest</code>.</td></tr><tr><td><code>GCP_SECRET_LOCATION</code></td><td><code>gcpsecret</code></td><td>Default <code>global</code>.</td></tr><tr><td><code>GCP_SECRET_VERSION</code></td><td><code>gcpsecret</code></td><td>Default <code>latest</code>.</td></tr><tr><td><code>GCP_SECRET_ADDITIONAL</code></td><td><code>gcpsecret</code></td><td>Comma-separated list of shared secret ids loaded BEFORE the app-specific secret, e.g. <code>generic,keycloak</code>.</td></tr></tbody></table><h2 id="config-file" tabindex="-1">Config file <a class="header-anchor" href="#config-file" aria-label="Permalink to “Config file”">​</a></h2><p>Set <code>CONFIG_FILE=/path/to/pika.yaml</code> to load a config file. Without it, the file loader looks for <code>pika.{toml,yaml,yml,json}</code> first in the current directory, then in <code>/etc/</code>. Env vars still override individual keys after the file is parsed.</p><p>A representative full config (all top-level keys correspond to the fields of <code>internal/config/config.go</code>):</p><div class="language-yaml"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">log_level</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">info</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  host</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;8080&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  base_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  tls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    cert_file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # empty = auto-managed self-signed cert under storage path</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    key_file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">storage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  bw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">data/pika</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    in_memory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Optional. When set, pika auto-unlocks (or auto-initializes) the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># at-rest encryption layer on boot. Leave empty to keep the original</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># &quot;operator unlocks through the UI on every restart&quot; flow.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Also readable via PIKA_ENCRYPTION_PASSWORD. See guide/encryption.md.</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">encryption</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  password</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">cluster</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  dns_addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pika-cluster.pika.svc.cluster.local</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  bind_addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0.0.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  replicas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  refresh_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">30s</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  heartbeat_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">5s</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  heartbeat_timeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">30s</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  security_key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;long-random-pre-shared-key&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  lock_key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pika-leader</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  sync_interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">5m</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  prefix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pika</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  forward_timeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">30s</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># OpenTelemetry — see github.com/rakunlabs/tell for the full schema.</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">telemetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  service</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pika</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>Most production deployments only set a handful of these. The Docker Compose example in <a href="./installation">Installation</a> uses just <code>PIKA_LOG_LEVEL</code>. Authentication, login UI, cookie behaviour, token issuance, OAuth2/LDAP/passkey providers, and rate limits are <strong>runtime settings</strong> — configure them under <strong>Settings → Authentication</strong>, not in this file.</p></div><h2 id="at-rest-encryption-passphrase" tabindex="-1">At-rest encryption passphrase <a class="header-anchor" href="#at-rest-encryption-passphrase" aria-label="Permalink to “At-rest encryption passphrase”">​</a></h2><p>The optional <code>encryption.password</code> field (or <code>PIKA_ENCRYPTION_PASSWORD</code> env var) controls how the at-rest encryption layer behaves at boot:</p><ul><li><strong>Empty (default)</strong> — original flow. If a verifier already exists on disk the server starts LOCKED and the operator unlocks through the UI on every restart.</li><li><strong>Set + server already initialized</strong> — pika auto-unlocks with the supplied passphrase. A wrong value is non-fatal: the server stays LOCKED, the UnlockScreen still asks for the real key, and <code>/api/v1/info</code> carries a warning flag so the SPA can call out the bad config value.</li><li><strong>Set + fresh install</strong> — pika auto-initializes the encryption layer using the supplied passphrase as the at-rest key. Same effect as clicking &quot;Initialize&quot; under <strong>Settings → Server encryption key</strong>.</li></ul><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>Populating <code>encryption.password</code> undoes the &quot;key never lives on disk&quot; property of the default flow. The field is masked from the startup <code>loaded configuration</code> log line (<code>log:&quot;-&quot;</code>), but it is readable to anyone who can read the config file or <code>/proc/$pid/environ</code>. Operators trading manual-unlock UX for at-rest-key-on-disk should accept that trade-off explicitly. See <a href="./encryption">Encryption</a> for the full lifecycle.</p></div><h2 id="external-config-sources" tabindex="-1">External config sources <a class="header-anchor" href="#external-config-sources" aria-label="Permalink to “External config sources”">​</a></h2><div class="info custom-block"><p class="custom-block-title custom-block-title-default">INFO</p><p>This section is about loading <strong>pika&#39;s own bootstrap config</strong> from Vault/Consul/GCP. It is not the same as <strong>config inheritance</strong>, which is the runtime feature that pulls values into served configs from those same systems. For inheritance see the <a href="./inheritance/">Inheritance</a> section.</p></div><p>The external loaders are wired in at compile time by importing them with <code>_ &quot;…&quot;</code> in <code>internal/config/config.go</code>. Each one runs only when its trigger env var is present and silently skips otherwise. The payloads from every active source are merged into the same struct in the order shown in <a href="#how-config-is-loaded">How config is loaded</a>, so you can layer &quot;shared baseline&quot; + &quot;overrides&quot; cleanly.</p><h3 id="vault" tabindex="-1">Vault <a class="header-anchor" href="#vault" aria-label="Permalink to “Vault”">​</a></h3><p>Reads a KVv2 secret whose data fields map onto <code>Config</code>.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VAULT_ADDR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://vault.internal:8200</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VAULT_SECRET_BASE_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">secret/apps          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># KVv2 mount path</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># AppRole login (optional — VAULT_TOKEN or Vault Agent also work)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VAULT_ROLE_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VAULT_ROLE_SECRET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...</span></span></code></pre></div><p>Pika fetches <code>secret/apps/pika</code> (KVv2). The fields stored at that path are merged into <code>Config</code> — e.g. a Vault secret with <code>log_level: debug</code> and <code>encryption.password: &lt;value&gt;</code> is enough to drive both.</p><h3 id="consul" tabindex="-1">Consul <a class="header-anchor" href="#consul" aria-label="Permalink to “Consul”">​</a></h3><p>Reads a single KV entry whose value is YAML/JSON.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CONSUL_HTTP_ADDR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://consul.internal:8500</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CONSUL_CONFIG_PATH_PREFIX</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config/prod       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional</span></span></code></pre></div><p>Pika fetches the KV value at <code>config/prod/pika</code> (or just <code>pika</code> without the prefix), decodes it as YAML by default, and merges it into <code>Config</code>. Standard Consul auth env vars (<code>CONSUL_HTTP_TOKEN</code>, mTLS variables) are honoured by the underlying client.</p><h3 id="gcp-parameter-manager" tabindex="-1">GCP Parameter Manager <a class="header-anchor" href="#gcp-parameter-manager" aria-label="Permalink to “GCP Parameter Manager”">​</a></h3><p>Renders a parameter version and merges the payload.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GOOGLE_APPLICATION_CREDENTIALS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/path/to/sa.json   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or workload identity</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_PROJECT_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my-project</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_PARAMETER_LOCATION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">global       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default; or a region</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_PARAMETER_VERSION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">latest        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default</span></span></code></pre></div><p>The parameter id is the configuration name (<code>pika</code>). <code>/</code> characters in the name are replaced with <code>-</code> because GCP parameter ids don&#39;t allow <code>/</code>. Parameter Manager renders the payload server-side, so referenced secrets are resolved before pika sees the bytes. Decoded as YAML by default.</p><h3 id="gcp-secret-manager" tabindex="-1">GCP Secret Manager <a class="header-anchor" href="#gcp-secret-manager" aria-label="Permalink to “GCP Secret Manager”">​</a></h3><p>Accesses one or more secret versions and merges their payloads.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GOOGLE_APPLICATION_CREDENTIALS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/path/to/sa.json</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_PROJECT_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">my-project</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_SECRET_LOCATION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">global          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_SECRET_VERSION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">latest           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Optional shared secrets loaded BEFORE the app-specific one; later wins.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GCP_SECRET_ADDITIONAL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">generic,keycloak</span></span></code></pre></div><p>Loads (in order):</p><ol><li>Each id in <code>GCP_SECRET_ADDITIONAL</code> — missing ones are silently skipped. Useful for mounting &quot;common credentials shared across multiple apps&quot;.</li><li>The app-specific secret named <code>pika</code>.</li></ol><p>If none of these exist the loader skips entirely. Decoded as YAML by default.</p><h3 id="http" tabindex="-1">HTTP <a class="header-anchor" href="#http" aria-label="Permalink to “HTTP”">​</a></h3><p>Fetches the config from an HTTP endpoint that returns JSON/YAML/TOML.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CONFIG_HTTP_ADDR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://config.internal/v1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CONFIG_HTTP_SUFFIX</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/raw            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional, appended after /pika</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CONFIG_HTTP_QUERY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">env</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">prod         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional query string</span></span></code></pre></div><p>Pika issues <code>GET \${CONFIG_HTTP_ADDR}/pika\${CONFIG_HTTP_SUFFIX}?\${CONFIG_HTTP_QUERY}</code>. <code>200</code> is parsed by <code>Content-Type</code>; <code>204</code> / <code>404</code> are treated as &quot;not configured here&quot; and the loader is skipped.</p><h2 id="https" tabindex="-1">HTTPS <a class="header-anchor" href="#https" aria-label="Permalink to “HTTPS”">​</a></h2><p>The main UI/API listener serves HTTPS by default. If no certificate exists at startup, Pika creates a self-signed ECDSA certificate under the storage directory and uses it immediately. Manage the active certificate from <strong>Settings → Certificates</strong>:</p><ul><li>Generate a replacement self-signed certificate with custom DNS/IP SANs.</li><li>Upload a PEM certificate chain and matching private key.</li><li>Allow plaintext HTTP on the same main port when running behind a trusted TLS-terminating proxy.</li><li>Disable HTTPS from the UI only when plaintext HTTP is enabled, to avoid locking yourself out.</li></ul><p>Set <code>server.tls.enabled: false</code> only when you want the process to be HTTP-only from startup.</p><h2 id="endpoints" tabindex="-1">Endpoints <a class="header-anchor" href="#endpoints" aria-label="Permalink to “Endpoints”">​</a></h2><p>Pika can expose additional listeners that serve configuration data in operator-chosen wire shapes — either a Consul KV-compatible read API or a custom Go-template response. Each endpoint binds its own <code>host:port</code>, owns its own auth setting, can run HTTPS using the managed certificate, can run an optional request-check stage, and is configured at runtime from <strong>Settings → Endpoints</strong>.</p><p>See <a href="/pika/guide/endpoints">Endpoints</a> for the wire format, template variables, and authentication options.</p><h2 id="authentication-sessions-and-cookies" tabindex="-1">Authentication, sessions, and cookies <a class="header-anchor" href="#authentication-sessions-and-cookies" aria-label="Permalink to “Authentication, sessions, and cookies”">​</a></h2><p>Authentication strategies (local, OAuth2, LDAP, passkey, header/proxy), session TTL, cookie behaviour, token issuance, and rate limiting are <strong>runtime settings</strong>, not process-level config — there is no <code>server.auth</code> block in <code>Config</code>. Configure them under <strong>Settings → Authentication</strong> in the UI, or via <code>PUT /api/v1/settings</code> (see <a href="/pika/guide/api">Admin API</a>). Changes apply without restarting.</p><h2 id="reverse-proxies-and-base-paths" tabindex="-1">Reverse proxies and base paths <a class="header-anchor" href="#reverse-proxies-and-base-paths" aria-label="Permalink to “Reverse proxies and base paths”">​</a></h2><p>If you serve pika under a sub-path (e.g. <code>https://example.com/pika/</code>), set:</p><div class="language-yaml"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  base_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/pika</span></span></code></pre></div><p>Pika rewrites internal links and SPA routing accordingly. Use a leading slash and no trailing slash; <code>/pika/</code> is normalized to <code>/pika</code>. If your proxy strips the prefix before forwarding to Pika, leave <code>server.base_path</code> unset. Make sure your proxy forwards the original <code>Host</code> header.</p><h2 id="cli" tabindex="-1">CLI <a class="header-anchor" href="#cli" aria-label="Permalink to “CLI”">​</a></h2><p>The pika binary takes a single optional flag (<code>--config</code>) and otherwise has <strong>no subcommands</strong>. All operational concerns — backups, key rotation, user management — are exposed through the <a href="/pika/guide/api">admin API</a> and the UI.</p>`,65)])])}const g=s(n,[["render",d]]);export{k as __pageData,g as default};
