apiVersion: v1
kind: ConfigMap
metadata:
  name: pika
data:
  pika.yaml: |
    log_level: info
    server:
      port: "8080"
      auth:
        session_ttl: 24h
        cookie:
          secure: true
          same_site: lax
    storage:
      bw:
        path: /data/pika
